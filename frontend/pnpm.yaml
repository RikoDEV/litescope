overrides:
  # baseline-browser-mapping@2.10.33 was flagged by the pnpm supply-chain
  # minimumReleaseAge policy (published seconds before CI ran).
  # Pin to the previous stable release.
  baseline-browser-mapping: "2.10.32"
